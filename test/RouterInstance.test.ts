describe("RouterInstance", () => {
  it("should get routes from URL", () => {});

  it("should emit current and previous route", () => {});

  it("should emit current and previous route", () => {});
});
